import '@testing-library/jest-dom';
// You can add other global setup code here if needed, for example:
// - Mocking global objects (fetch, localStorage, etc.)
// - Setting up a global MSW (Mock Service Worker) server for API mocking

// Example: Mocking a global function (if you had one)
// global.myGlobalFunction = vi.fn();

// Clean up after each test (optional, Vitest does some cleanup by default)
// import { afterEach } from 'vitest';
// import { cleanup } from '@testing-library/react';
// afterEach(() => {
//   cleanup(); // Cleans up JSDOM rendered components
// });
